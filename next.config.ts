import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The floating dev badge sits over the bottom-left of the layout, which is
  // where the sign-up line and drill content land on a phone viewport.
  devIndicators: false,
};

export default nextConfig;
